/*import express from 'express';
import { productList, changeAvailablity, productDashboard, productProfile } from '../controllers/productController.js';
import authProduct from '../middleware/authProduct.js';
const productRouter = express.Router();


productRouter.post("/cancel-appointment", authProduct, appointmentCancel)
productRouter.get("/appointments", authProduct, appointmentsProd)
productRouter.get("/list", productList)
productRouter.post("/change-availability", authProduct, changeAvailablity)
productRouter.post("/complete-appointment", authProduct, appointmentComplete)
productRouter.get("/dashboard", authProduct, productDashboard)
productRouter.get("/profile", authProduct, productProfile)
productRouter.post("/update-profile", authProduct, updateProdProfile)

export default productRouter;*/